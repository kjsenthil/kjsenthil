<policies>
  <inbound>
    <base />
    
    %{ if config.backend_url != ""}
      <set-backend-service base-url="${config.backend_url}" />
    %{ endif ~}
    %{ if config.allow-credentials != null}
    <cors allow-credentials="${config.allow-credentials}">
     %{ else ~}
     <cors>
     %{ endif ~}

     %{ if config.cors_allowed_origins != null }
        <allowed-origins>
          %{ for origin in config.cors_allowed_origins }
            <origin>${ origin }</origin>
          %{ endfor ~}
        </allowed-origins>
      %{ endif ~}

      %{ if config.cors_allowed_method != null }
        <allowed-methods>
          <method>${ config.cors_allowed_method }</method>
      </allowed-methods>
      %{ endif ~}

      %{ if config.cors_allowed_headers != null }
        <allowed-headers>
          %{ for header in config.cors_allowed_headers }
            <header>${ header }</header>
          %{ endfor ~}
        </allowed-headers>
      %{ endif ~}

      %{ if config.cors_exposed_headers != null }
        <expose-headers>

            <header>${ config.cors_exposed_headers }</header>

        </expose-headers>
      %{ endif ~}

    </cors>

    %{ if config.headers != null}
      %{ for setheader in config.headers  }
      <set-header name="${setheader.name}" exists-action="${setheader.exists_action}">
        <value>${ setheader.value }</value>
      </set-header>
      %{ endfor ~}
    %{ endif ~}

  </inbound>
  <backend>
    <base />
  </backend>
  <outbound>
    %{ if config.outbound_headers != null}
      %{ for setheader in config.outbound_headers  }
      <set-header name="${setheader.name}" exists-action="${setheader.exists_action}">
        <value>${ setheader.value }</value>
      </set-header>
      %{ endfor ~}
    %{ endif ~}  
    <base />
  </outbound>
  <on-error>
    <base />
  </on-error>
</policies >