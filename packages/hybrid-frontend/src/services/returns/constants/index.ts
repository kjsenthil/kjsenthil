/* eslint-disable import/prefer-default-export */
export enum ReturnsApiValidationErrors {
  CODE_PREFIX = 'val-annualreturns',
  SAME_SIGN = 'Value cannot be calculated due to signs of both lower and upper rates to be the same',
}
