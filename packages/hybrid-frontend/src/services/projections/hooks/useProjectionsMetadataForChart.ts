import { ProjectionsChartMetadata } from '../types';

export default function useProjectionsMetadataForChart(): ProjectionsChartMetadata {
  // TODO: replace with actual API data when it's ready

  return { goalMet: true, zeroValueYear: 2086, todayAge: 31 };
}
