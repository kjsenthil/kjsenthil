const APP_HEADING_TEXT = 'Digital Hybrid';

describe('Index page', () => {
  before(() => {
    cy.visit('/');
  });

  it('has a title', () => {
    // Dummy test until we have proper tests
  });
});
